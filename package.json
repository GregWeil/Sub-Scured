{
  "name": "submarines",
  "version": "0.1.0",
  "description": "Use the arrow keys to navigate, using your sonar's latent image to collect treasure and avoid mines.",
  "scripts": {
    "build": "tsc --noEmit && esbuild src/client.ts --bundle --outdir=public/build",
    "serve": "esbuild src/client.ts --bundle --outdir=public/build --serve --servedir=public"
  },
  "dependencies": {
    "@types/howler": "^2.2.12",
    "@types/three": "^0.144.0",
    "esbuild": "^0.25.6",
    "howler": "^2.2.4",
    "segment-intersection": "^0.0.1",
    "three": "^0.145.0",
    "typescript": "^5.8.3"
  },
  "repository": {
    "url": "https://glitch.com/edit/#!/submarines"
  },
  "license": "MIT",
  "keywords": [
    "node",
    "glitch"
  ]
}
